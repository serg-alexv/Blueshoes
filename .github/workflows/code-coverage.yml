name: code-coverage

on:
  push:
  pull_request:

permissions: {}

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: No-op (docs-only)
        run: echo "Docs-only repository: no code coverage job configured."
